mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile,autoplay=true,controls="controls")
        h4.videoBlock__title=video.title
        h6.videoBlock__views=video.views
    
